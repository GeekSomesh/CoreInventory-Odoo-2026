PORT=3001
JWT_SECRET=replace-with-a-long-random-secret

# Leave as lowdb for the current JSON-backed demo.
DB_CLIENT=lowdb

# Set DB_CLIENT=postgres and provide DATABASE_URL to use PostgreSQL.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/coreinventory

# Optional for managed PostgreSQL providers.
DB_SSL=false
