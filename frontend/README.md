# CoreInventory Frontend

React + TypeScript + Vite frontend for the CoreInventory IMS.

## Scripts

```powershell
npm run dev
npm run build
npm run lint
npm run preview
```

## API Dependency

The app expects backend API at:

`http://localhost:3001/api`

Configured in:

`src/api/client.ts`
